# Discovered References (Template)

Use this file to persist confirmed reference names for one API module.

## Rules

- Store reference names only (env var names, Key Vault names, secret names).
- Never store secret values, tokens, keys, passwords, or connection strings.
- Mark unknown items as `pending`.

## Suggested Fields

- `api_base_url_env`: `pending`
- `key_vault_name`: `pending`
- `bearer_env_name`: `pending`
- `bearer_secret_name`: `pending`
- `api_key_env_name`: `pending`
- `notes`: ``
